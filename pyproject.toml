[project]
name = "pi5-car-detector"
version = "0.1.0"
description = "Realtime vehicle detection baseline for Raspberry Pi 5."
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
